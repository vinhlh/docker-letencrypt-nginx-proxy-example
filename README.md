# docker-letencrypt-nginx-proxy-example
An example using nginx-proxy, letsencrypt, letsencrypt-nginx-proxy-companion

## Debugging
`docker logs nginx`

`docker exec -it nginx-gen cat /etc/nginx/conf.d/default.conf`
